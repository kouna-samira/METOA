package com.groupe2.METOA.gestionProfilUtiisateur.entity.fonctionaleterAvances.chatsMessageries.message.entity;

public enum MessageType {
    TEXT,   // Message textuel classique
    IMAGE,  // Image (jpg, png, gif, webp, etc.)
    FILE,   // Fichier générique / Document (pdf, docx, zip, etc.)
    AUDIO   // Message vocal / Fichier audio (optionnel)
}
